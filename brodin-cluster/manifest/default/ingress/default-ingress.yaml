apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: brodin-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: brodin-webserver-service
            port:
              name: web